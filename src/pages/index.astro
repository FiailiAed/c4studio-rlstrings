---
import MainLayout from '../layouts/MainLayout.astro';
import { ShieldCheck, ChevronRight, Box, Grid3x3, AlignVerticalSpaceAround, Hammer, ArrowRight } from 'lucide-react';
---

<MainLayout
  title="RL Strings | Elite Custom Lacrosse Stringing"
  description="Stop settling for factory trash. Get the precision, consistency, and hold that elite players demand. Custom built, professionally strung."
>
  <!-- Hero Section -->
  <section class="relative pt-20 pb-20 px-4">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Elite Badge -->
      <div class="inline-flex items-center gap-2 px-4 py-2 bg-red-950/50 border border-red-900/50 rounded-full mb-8">
        <span class="relative flex h-3 w-3">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-500 opacity-75"></span>
          <span class="relative inline-flex rounded-full h-3 w-3 bg-red-600"></span>
        </span>
        <span class="text-red-200 text-sm font-bold uppercase tracking-wider">Elite Craftsmanship by Todd Ruff</span>
      </div>

      <!-- Headline -->
      <h1 class="text-6xl md:text-8xl font-black tracking-tighter mb-6 leading-[0.9] uppercase">
        Stop Settling for <span class="text-transparent bg-clip-text bg-gradient-to-br from-red-500 to-red-700">Factory Trash.</span>
      </h1>

      <!-- Subheadline -->
      <p class="text-xl text-neutral-400 mb-12 max-w-2xl mx-auto font-medium leading-relaxed">
        Your pocket is the most important piece of equipment you own. Get the precision, consistency, and hold that elite players demand. Custom built, professionally strung.
      </p>

      <!-- CTA -->
      <div class="flex flex-col sm:flex-row justify-center items-center gap-4">
        <a
          href="/shop/builder"
          class="glow-red group relative px-8 py-5 bg-red-600 hover:bg-red-700 text-white font-black text-lg rounded-xl transition-all active:scale-[0.98] uppercase tracking-wide flex items-center gap-3 shadow-xl shadow-red-900/20 overflow-hidden"
        >
          <span class="relative z-10">Start Your Build</span>
          <ChevronRight className="w-6 h-6 relative z-10 group-hover:translate-x-1 transition-transform" client:load />
          <div class="absolute inset-0 h-full w-full scale-x-0 group-hover:scale-x-100 transition-transform origin-left bg-gradient-to-r from-transparent via-white/20 to-transparent"></div>
        </a>
        <p class="text-neutral-500 text-sm font-medium mt-2 sm:mt-0 flex items-center gap-1">
          <ShieldCheck className="w-4 h-4 text-red-500" client:load />
          100% Satisfaction Guarantee
        </p>
      </div>
    </div>
  </section>

  <!-- Elite Build Process Section -->
  <section class="py-20 px-4 relative z-10">
    <div class="max-w-6xl mx-auto">
      <!-- Section Header -->
      <div class="text-center mb-16">
        <h2 class="text-3xl font-black uppercase tracking-tight mb-4">The Elite Build Process</h2>
        <p class="text-neutral-400 max-w-xl mx-auto">We've simplified the process of getting a pro-level pocket. You pick the parts, we provide the elite labor.</p>
      </div>

      <!-- 4-Step Grid -->
      <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
        <!-- Step 1: Select Head -->
        <div class="glass-dark p-8 rounded-3xl border-t-4 border-t-red-600 relative overflow-hidden group hover:-translate-y-1 transition-all duration-300">
          <div class="absolute -right-4 -top-4 text-8xl font-black text-neutral-900/50 z-0 select-none">1</div>
          <div class="relative z-10">
            <div class="w-14 h-14 bg-red-900/30 rounded-xl flex items-center justify-center mb-6 border border-red-800/50 group-hover:border-red-500 transition-colors">
              <Box className="w-8 h-8 text-red-500" client:load />
            </div>
            <h3 class="text-xl font-bold mb-2 uppercase">Select Head</h3>
            <p class="text-neutral-400 text-sm">Choose from top-tier unstrung heads or bring your own.</p>
          </div>
        </div>

        <!-- Step 2: Select Mesh -->
        <div class="glass-dark p-8 rounded-3xl border-t-4 border-t-neutral-800 relative overflow-hidden group hover:-translate-y-1 transition-all duration-300">
          <div class="absolute -right-4 -top-4 text-8xl font-black text-neutral-900/50 z-0 select-none">2</div>
          <div class="relative z-10">
            <div class="w-14 h-14 bg-neutral-900/50 rounded-xl flex items-center justify-center mb-6 border border-neutral-800 group-hover:border-white/50 transition-colors">
              <Grid3x3 className="w-8 h-8 text-white" client:load />
            </div>
            <h3 class="text-xl font-bold mb-2 uppercase">Select Mesh</h3>
            <p class="text-neutral-400 text-sm">Performance mesh options for the perfect feel and hold.</p>
          </div>
        </div>

        <!-- Step 3: Select Strings -->
        <div class="glass-dark p-8 rounded-3xl border-t-4 border-t-neutral-800 relative overflow-hidden group hover:-translate-y-1 transition-all duration-300">
          <div class="absolute -right-4 -top-4 text-8xl font-black text-neutral-900/50 z-0 select-none">3</div>
          <div class="relative z-10">
            <div class="w-14 h-14 bg-neutral-900/50 rounded-xl flex items-center justify-center mb-6 border border-neutral-800 group-hover:border-white/50 transition-colors">
              <AlignVerticalSpaceAround className="w-8 h-8 text-white" client:load />
            </div>
            <h3 class="text-xl font-bold mb-2 uppercase">Select Strings</h3>
            <p class="text-neutral-400 text-sm">Customize your colors for sidewalls and shooters.</p>
          </div>
        </div>

        <!-- Step 4: Pro Labor -->
        <div class="glass-dark p-8 rounded-3xl border-t-4 border-t-red-600 bg-gradient-to-b from-red-950/20 to-transparent relative overflow-hidden shadow-xl shadow-red-900/10">
          <div class="absolute -right-4 -top-4 text-8xl font-black text-red-900/20 z-0 select-none">4</div>
          <div class="relative z-10">
            <div class="w-14 h-14 bg-red-600 rounded-xl flex items-center justify-center mb-6 shadow-lg shadow-red-600/30">
              <Hammer className="w-8 h-8 text-white" client:load />
            </div>
            <h3 class="text-xl font-black mb-2 uppercase text-red-100">Pro Labor Included</h3>
            <p class="text-red-200/80 text-sm font-medium">Every order includes expert stringing by Todd Ruff. <span class="underline decoration-red-500/50">Non-negotiable quality.</span></p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Final CTA Section -->
  <section class="py-20 px-4 text-center">
    <h2 class="text-4xl font-black uppercase mb-8">Ready to upgrade your game?</h2>
    <a
      href="/shop/builder"
      class="glow-red group relative px-8 py-4 bg-red-600 hover:bg-red-700 text-white font-black text-lg rounded-xl transition-all active:scale-[0.98] uppercase tracking-wide inline-flex items-center gap-3"
    >
      Enter The Builder
      <ArrowRight className="w-6 h-6 group-hover:translate-x-1 transition-transform" client:load />
    </a>
  </section>
</MainLayout>
