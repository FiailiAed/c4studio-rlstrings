---
export const prerender = true;

import ShopLayout from '../layouts/ShopLayout.astro';
---

<ShopLayout
  title="RL Strings | Cart"
  description="Your builder is your cart — select your options and check out directly."
>
  <section class="flex flex-col items-center justify-center min-h-[60vh] px-4 py-16 text-center">
    <div class="w-full max-w-md space-y-6">

      <div class="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto">
        <svg class="w-8 h-8 text-gray-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="9" cy="21" r="1" />
          <circle cx="20" cy="21" r="1" />
          <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6" />
        </svg>
      </div>

      <div>
        <h1 class="text-2xl font-black text-gray-900 mb-2">Your Builder is Your Cart</h1>
        <p class="text-gray-500">
          RL Strings doesn't use a traditional cart. Select your service, head, mesh, strings, and pocket depth in the builder — then check out directly with Stripe.
        </p>
      </div>

      <a
        href="/shop"
        class="inline-flex items-center justify-center gap-2 bg-black text-white px-8 py-4 rounded-lg font-bold text-lg hover:bg-gray-800 transition w-full"
      >
        Go to Builder
        <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="5" y1="12" x2="19" y2="12" />
          <polyline points="12 5 19 12 12 19" />
        </svg>
      </a>

    </div>
  </section>
</ShopLayout>
